{% extends 'base.html.twig' %}

{% block title %}Nouvelle demande de maintenance{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>➕ Nouvelle demande de maintenance</h1>

    {{ form_start(form) }}
        <div class="card mb-4">
            <div class="card-header bg-dark text-white">Informations du client</div>
            <div class="card-body">
                {{ form_row(form.fullName) }}
                {{ form_row(form.email) }}
                {{ form_row(form.phone) }}
                {{ form_row(form.addressLine1) }}
                {{ form_row(form.addressLine2) }}
                {{ form_row(form.postalCode) }}
                {{ form_row(form.city) }}
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header bg-primary text-white">Détails de la demande</div>
            <div class="card-body">
                {{ form_row(form.type) }}
                {{ form_row(form.requestedDate) }}
                {{ form_row(form.comment) }}
                {{ form_row(form.photos) }}
            </div>
        </div>

        <button class="btn btn-success">💾 Enregistrer</button>
        <a href="{{ path('app_maintenance_request_index') }}" class="btn btn-secondary">⬅ Retour</a>
    {{ form_end(form) }}
</div>
{% endblock %}
