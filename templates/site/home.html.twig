{% extends 'base.html.twig' %}

{% block title %}Énergie Solaire - Votre Indépendance{% endblock %}

{% block body %}
    <section class="relative h-screen bg-cover bg-center" style="background-image:url('/images/solar-panels.jpg')">
        <div class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center">
            <div class="text-center text-white">
                <h1 class="text-5xl font-bold">Passez au solaire</h1>
                <p class="mt-4 text-xl">Économisez, gagnez en autonomie, protégez la planète.</p>
                <a href="#devis" class="mt-6 inline-block bg-yellow-500 px-6 py-3 text-lg rounded shadow-lg">Simuler mes économies</a>
            </div>
        </div>
    </section>

    <section id="temoignages" class="py-16 bg-gray-100">
        <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-3xl font-bold mb-8">Ils nous font confiance</h2>
            <div class="grid md:grid-cols-3 gap-6">
                <blockquote class="bg-white p-6 rounded shadow">
                    <p>"Grâce aux panneaux, ma facture a chuté de 60%."</p>
                    <footer class="mt-2 font-semibold">— Jean, Lyon</footer>
                </blockquote>
                <blockquote class="bg-white p-6 rounded shadow">
                    <p>"Installation rapide et équipe très professionnelle."</p>
                    <footer class="mt-2 font-semibold">— Sophie, Bordeaux</footer>
                </blockquote>
                <blockquote class="bg-white p-6 rounded shadow">
                    <p>"J’ai enfin une maison autonome en énergie."</p>
                    <footer class="mt-2 font-semibold">— Marc, Toulouse</footer>
                </blockquote>
            </div>
        </div>
    </section>

    <section id="devis" class="py-16">
        <div class="max-w-lg mx-auto">
            <h2 class="text-3xl font-bold text-center mb-6">Demandez votre devis gratuit</h2>
            {{ form_start(contactForm) }}
                {{ form_row(contactForm.nom) }}
                {{ form_row(contactForm.email) }}
                {{ form_row(contactForm.telephone) }}
                {{ form_row(contactForm.message) }}
                {{ form_row(contactForm.envoyer) }}
            {{ form_end(contactForm) }}
        </div>
    </section>
{% endblock %}
