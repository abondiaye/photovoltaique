{% extends 'admin/layout.html.twig' %}

{% block title %}Admin · Maintenance{% endblock %}

{% block admin_body %}
<h1>🛠 Demandes de maintenance</h1>
<a href="{{ path('app_maintenance_request_new') }}" class="btn btn-success mb-3">➕ Nouvelle demande</a>
<table class="table table-bordered table-striped">
    <thead class="table-dark">
    <tr>
        <th>ID</th><th>Client</th><th>Ville</th><th>Type</th><th>Statut</th><th>Date souhaitée</th><th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for request in requests %}
        <tr>
            <td>#{{ request.id }}</td>
            <td>{{ request.fullName }}</td>
            <td>{{ request.city }}</td>
            <td>{{ request.type }}</td>
            <td>{{ request.status }}</td>
            <td>{{ request.requestedDate|date('d/m/Y H:i') }}</td>
            <td>
                <a class="btn btn-sm btn-info" href="{{ path('app_maintenance_request_show', {id: request.id}) }}">Voir</a>
                <a class="btn btn-sm btn-warning" href="{{ path('app_maintenance_request_edit', {id: request.id}) }}">Modifier</a>
            </td>
        </tr>
    {% else %}
        <tr><td colspan="7" class="text-center">Aucune demande</td></tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
