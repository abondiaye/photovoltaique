### Production environment ###
APP_ENV=prod
APP_DEBUG=0

# Garde la même clé si déjà déployée, sinon remplace par une clé aléatoire forte
APP_SECRET=6a3d74b7c8ff0d82e91e0f8b9e76a3e1

# MariaDB 10.11.9 locale, base 'voltaique'
APP_ENV=prod
APP_DEBUG=0
DATABASE_URL="mysql://volt_user:S3cur3!112011@10.0.0.12:3306/voltaique?serverVersion=10.11.9-MariaDB&charset=utf8mb4"
MESSENGER_TRANSPORT_DSN=doctrine://default

# Mailer - exemples courants (choisis 1 et remplace par tes vraies valeurs)
# MAILER_DSN=smtp://user:pass@smtp.gmail.com:587
# MAILER_DSN=smtp://user:pass@smtp-relay.sendinblue.com:587
# MAILER_DSN=smtp://user:pass@smtp.mailgun.org:587
MAILER_DSN=smtp://user_smtp:mot_de_passe_smtp@mail.example.com:587

# Optionnel : si tu utilises HttpCache de Symfony (Kernel::getHttpCache)
APP_HTTP_CACHE=true

# Recommandés en prod derrière un proxy (à ajuster si Nginx/Apache reverse-proxy)
# TRUSTED_PROXIES=127.0.0.1,::1
# TRUSTED_HOSTS=^example\\.com$
# LOG_LEVEL=error
